; qmllint reads this for every file under this directory. Categories not
; listed keep qmllint's defaults; see `qmllint --write-defaults`.
[General]
; Any warning fails the lint, and the CI step with it.
MaxWarnings=0

[Warnings]
; The remaining unqualified names are the _myGameModel and _myPiecesModel
; context properties and the enums registered by hand. Both are being
; replaced (see features/2026/09/qml-cleanups.md); until then they are
; reported but do not fail the lint. Delete this line once they are gone.
UnqualifiedAccess=info
